// The configurations sidebar was merged into `SettingsView` as part of the
// unified NavigationSplitView. This file intentionally left empty so xcodegen
// still sees the source tree without a dangling reference to a renamed file.
